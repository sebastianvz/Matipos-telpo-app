<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    tools:context=".ui.activity.DeviceSettingsActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="20dp">

        <TextView
            android:textColor="@color/colorAccent"
            android:id="@+id/textView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Devices Settings"
            android:textAlignment="center"
            android:textStyle="bold"
            android:layout_marginBottom="20dp"
            android:textSize="30dp" />

        <com.google.android.material.switchmaterial.SwitchMaterial
            android:textColor="@color/colorAccent"
            android:id="@+id/switchFaceRecognition"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:checked="true"
            android:text="Reconocimiento facial"/>

        <com.google.android.material.switchmaterial.SwitchMaterial
            android:textColor="@color/colorAccent"
            android:id="@+id/switchCameraLight"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:checked="true"
            android:text="Luz camara"/>

        <com.google.android.material.switchmaterial.SwitchMaterial
            android:textColor="@color/colorAccent"
            android:id="@+id/switchQrReader"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:checked="true"
            android:text="Lector QR"/>

        <com.google.android.material.switchmaterial.SwitchMaterial
            android:textColor="@color/colorAccent"
            android:id="@+id/switchNfcReader"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:checked="true"
            android:text="Lector NFC"/>


    </LinearLayout>

    <Button
        android:background="@drawable/buttom_style"
        android:layout_marginBottom="20dp"
        android:layout_gravity="center|bottom"
        android:id="@+id/btnUpdate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Actualizar"
        android:textSize="28sp"
        android:textColor="@android:color/white"
        android:textStyle="bold"/>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:tint="@android:color/white"
        android:id="@+id/btnBack"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|left"
        android:layout_margin="22dp"
        android:src="@drawable/baseline_arrow_back_24" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>